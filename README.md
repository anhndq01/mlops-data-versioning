# Data Versioning and Differential Privacy

Data versioning using LakeFS and DVC.

* Commands for setting up and version data using LakeFS are in `lakefs.sh`, and using DVC is in `DVC.sh`
* Notebooks run on data version 1 are in GitHub branch v1, while branch v2 contains results on dataset version 2.
